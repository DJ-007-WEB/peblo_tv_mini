services:
  - type: web
    name: peblo-api
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: .
    healthCheckPath: /health/ready
    plan: starter
    dockerCommand: alembic upgrade head && python -m app.seed /app/seed_shows.json && uvicorn app.main:app --host 0.0.0.0 --port 8000
    disk:
      name: peblo-storage
      mountPath: /app/storage
      sizeGB: 1
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: peblo-db
          property: connectionString
      - key: STORAGE_DIR
        value: /app/storage
      - key: CATALOG_PATH
        value: /app/storage/catalogue.json
      - key: ENVIRONMENT
        value: production
      - key: ALLOWED_ORIGINS
        value: https://peblo-cms.onrender.com,https://peblo-viewer.onrender.com
      - key: EDITOR_TOKEN
        sync: false
      - key: ADMIN_TOKEN
        sync: false

  - type: web
    name: peblo-cms
    runtime: docker
    dockerfilePath: ./cms/Dockerfile
    dockerContext: .
    plan: starter
    envVars:
      - key: VITE_API_BASE
        value: https://peblo-api.onrender.com
      - key: API_BASE_URL
        value: https://peblo-api.onrender.com

  - type: web
    name: peblo-viewer
    runtime: docker
    dockerfilePath: ./viewer/Dockerfile
    dockerContext: .
    plan: starter
    envVars:
      - key: VITE_API_BASE
        value: https://peblo-api.onrender.com
      - key: API_BASE_URL
        value: https://peblo-api.onrender.com

databases:
  - name: peblo-db
    plan: starter
    databaseName: peblo
    user: peblo
